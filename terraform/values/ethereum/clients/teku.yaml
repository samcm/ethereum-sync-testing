# fullnameOverride: SET_ME

image:
  tag: develop

# podLabels:
  # ethereum-instance: SET_ME

mode: "beacon"

resources:
  requests:
    cpu: "1"
    memory: "2000Mi"

p2pNodePort:
  enabled: true
  startAt: 31701

persistence:
  enabled: true
  size: 50Gi

extraArgs:
- --network=ropsten
- --ee-jwt-secret-file="/shared/jwtsecret"
